<h1>New approval</h1>

<%= form_for :approval, :url => post_comment_approval_path(@post, @comment) do |f| %>
  <%= f.error_messages %>
  <%= render :partial => 'fields', :locals => {:f => f} %>
  <%= f.submit %>
<% end %>
